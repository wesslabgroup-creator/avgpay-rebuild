export default function DataPolicyPage() {
  return <main className="max-w-4xl mx-auto px-6 py-12 space-y-4"><h1 className="text-3xl font-bold">Data Policy</h1><p>AvgPay combines self-reported submissions and public compensation datasets. We normalize records to remove obvious outliers and harmonize compensation fields.</p><p>We do not claim every submission is verified; users should interpret ranges alongside sample size and percentile spread.</p></main>;
}
